class Category {
  constructor({ id, category_name, category_description }) {
    this.id = id;
    this.name = category_name;
    this.description = category_description;
  }
}

export default Category;
